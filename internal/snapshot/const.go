package snapshot

const (
	// LabelProject is used to filter containers.
	LabelProject = "com.docker.compose.project"
	// LabelService is used to filter containers.
	LabelService = "com.docker.compose.service"
	// LabelVolume is used to filter volumes.
	LabelVolume = "com.docker.compose.volume"
)
